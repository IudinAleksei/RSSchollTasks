import { PAGE_ELEMENT } from './constants/constants';
import { setParams } from './utils/localStorage';
import messageForUser from './utils/messageForUser';


const CURRENT_STATE = {
};

const clickHandler = () => {
};

export const userEventHandler = () => {
};

export const initStartState = async () => {
};
