import renderWeather from './js/dom/weatherRender';

window.onload = () => {
  renderWeather();
  // console.log(await getGeolocation());
  // initStartState();
  // clickHandler();
  // keyboardHandler();
};
